name: Java CI (Gradle, Temurin, JDK 11)

on:
  push:
    branches: main
  pull_request:
    branches: main

jobs:
  build:
    runs-on: "ubuntu-latest"
  steps:
  - name: Run unit tests
    run: ./gradlew tests:test